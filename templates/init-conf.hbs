echo "Initializing {{project_name}}"
# Make a database, if we don't already have one
echo "Creating database (if it's not already there)"
mysql -u root --password=root -e 'CREATE DATABASE IF NOT EXISTS `{{project_name}}`'
mysql -u root --password=root -e 'GRANT ALL PRIVILEGES ON `{{project_name}}`.* TO wp@localhost IDENTIFIED BY `wp`;'

# The Vagrant site setup script will restart Nginx for us
echo " site now installed";