<?php

namespace {{#each namespaces}}{{#unless @last}}{{#unless @first}}\\{{/unless}}{{this}}{{/unless}}{{/each}};

class {{#each namespaces}}{{#if @last}}{{this}}{{/if}}{{/each}}View extends \CatalystWP\Nucleus\View
{

    public function initialize()
    {
        $this->template = 'ADD TEMPLATE NAME HERE';
    }
}